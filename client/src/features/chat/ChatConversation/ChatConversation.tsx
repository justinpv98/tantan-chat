import React from 'react'
import { styled } from '@/stitches.config'

import { Flex } from '@/features/ui'

type Props = {}

export default function ChatConversation({}: Props) {
  return (
    <Flex direction="column" css={{height: "100%", overflowY: "scroll"}}>
       
    </Flex>
  )
}